{
  "name": "sjhs-science-memory-palace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "audit": "node scripts/audit.mjs",
    "build": "node scripts/package-pages.mjs",
    "verify": "npm test && npm run audit && npm run build && node scripts/verify-generated.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
